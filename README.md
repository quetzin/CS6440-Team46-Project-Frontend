# ICU Wallboard 

- Patient Board (grid of patient status lights)
- Patient View (vitals + demographics)

## Quickstart
```bash
npm install
npm run dev
```

APIs
- `GET /api/patients`
- `GET /api/patients/:id`
